# Mercado Libre Provider

Provider inicial de Mercado Libre con laboratorio aislado para pruebas de navegador.

La primera etapa separa `browser`, `probes` y `runtime` para explorar el canal sin mezclarlo con la capa principal de produccion.
